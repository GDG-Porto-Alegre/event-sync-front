pre-commit:
  parallel: true
  commands:
    eslint:
      glob: "*.{js,ts,jsx,tsx}"
      exclude:
        - "node_modules"
        - src/route-tree.gen.ts
      run: pnpm eslint {staged_files} --fix
